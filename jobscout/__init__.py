"""jobscout — an agentic job finder that reads the applications you have already
written and goes looking for the next one.

Nothing personal lives in this package. Your materials stay in a folder you point
it at; its state lives in a data dir you choose. Both are outside the repo, and
:mod:`jobscout.config` refuses to start if they are not.
"""

__version__ = "0.1.0"
